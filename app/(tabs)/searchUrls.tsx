import BaseLayout from "@/components/BaseLayout";
import { Text } from "react-native-paper";

export default function SearchUrls() {
    return (
        <BaseLayout>
            <Text>Search urls</Text>
        </BaseLayout>
    )
}